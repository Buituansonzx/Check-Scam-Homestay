# Copy this file to `.env.development` (or another `.env.*`) and fill values.
# The active env file is selected by `ENV_FILE` (see `ENV_README.md`).

# Social auth redirect URLs (used in `src/components/AuthModal/AuthModal.tsx`)
GOOGLE_AUTH_URL=
FACEBOOK_AUTH_URL=

# API
# Example: http://localhost:8000/api
API_BASE_URL=
# Request timeout in ms (optional)
API_TIMEOUT_MS=15000
